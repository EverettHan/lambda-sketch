/**
 * This header only exists in order to avoid compilation errors in client frameworks
 * (CATClassAUI, FreeStyleConstraintSolver, FreeStyleResources, FreeStyleShapeModel).
 * Once these frameworks are migrated to CATPGMCreateAnalyticMatchSurface.h, the current header
 * must be deleted.
 */

#include "CATPGMCreateAnalyticMatchSurface.h"
