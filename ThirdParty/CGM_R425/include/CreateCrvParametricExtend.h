/**
 * This header only exists in order to avoid compilation errors in client frameworks (CATClassAUI, FreeStyleResources).
 * Once these frameworks are migrated to CATPGMCreateCrvParametricExtend.h, the current header
 * must be deleted.
 */

#include "CATPGMCreateCrvParametricExtend.h"
