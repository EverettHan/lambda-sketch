//===================================================================
// COPYRIGHT Dassault Systemes 2018/05/17
//===================================================================
// CATIKnowledgeProgressBarMath.cpp
// Header definition of class CATIKnowledgeProgressBarMath
//===================================================================
//
// Usage notes:
//
//===================================================================
//  2018/05/17 Creation: Code generated by the 3DS wizard
//===================================================================
#ifndef CATIKnowledgeProgressBarMath_H
#define CATIKnowledgeProgressBarMath_H

#include "KnowledgeItf.h"
#include "CATBaseUnknown.h"


extern ExportedByKnowledgeItf  IID IID_CATIKnowledgeProgressBarMath ;
class ExportedByKnowledgeItf CATIKnowledgeProgressBarMath: public CATBaseUnknown
{
CATDeclareInterface;

public:

    // Definir le nombre maximum de steps dans le programme
    virtual void SetMaxStep(int maxStep) = 0;

    // Passer au step suivant du programme
    virtual void NextStep() = 0;

    // Brider la valeur d'avancement du programme en cas de sous-programme
    virtual void SetNextValue(double value) = 0;

    // Definir l'avancement local du programme
    virtual void SetValue(double value) = 0;

    // Calculer le poids local relatif a chaque programme
    virtual void ComputeLocalWeight(int mode) = 0;

    // Calculer le poids global relatif a chaque programme
    virtual void ComputeGlobalWeight() = 0;

    // Calculer l'avancement local du programme
    virtual void ComputeLocalValue(int mode) = 0;

    // Calculer l'avancement global du programme
    virtual void ComputeGlobalValue() = 0;

    // Ajouter un niveau de profondeur au programme (un sous-programme)
    virtual void AddLevel() = 0;

    // Supprimer un niveau de profondeur au programme
    virtual void RemoveLevel() = 0;

    // Recuperer l'avancement global
    virtual double GetGlobalValue() = 0;

    // Recuperer l'avancement local (contextuel au niveau courant), identique a l'avancement globale si le programme n'a aucune profondeur
    virtual double GetLocalValue() = 0;
};

//-----------------------------------------------------------------------
CATDeclareHandler( CATIKnowledgeProgressBarMath, CATBaseUnknown );

#endif
