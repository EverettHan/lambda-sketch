//===================================================================
// COPYRIGHT DASSAULT SYSTEMES 2020/06/17
//===================================================================
// CATVidGridViewAutoCheckColumn.h
// Header definition of class CATVidGridViewAutoCheckColumn.h
//===================================================================
//
// Usage notes:
//
//===================================================================
//  2020/06/17 Creation: Code generated by the 3DS wizard  ISP
//===================================================================

#ifndef CATVidGridViewAutoCheckColumn_H
#define CATVidGridViewAutoCheckColumn_H

#include <CATVidGridViewColumn.h>
#include <VisuDialog.h>
#include <CATBaseUnknown.h>
#include <CATVid.h>
#include <CATBoolean.h>


/**
 * Define an auto-check column.
 *
 * This column is dedicated representing some content which can be checked.
 * Thid column only support representing some CATSYPDataBlock items.
 * In addition, represented datablocks in this column MUST OWN THE "Checked" property AND THE "Mixed" property.
 * To reference the model, both existing ways are working :
 * - Data/SetData (of the "Checked" property). In this case, to use the BindWithSelectionFlag = "1", you MUST set EditionTweakerDirectBindingFlag="1".
 * - CellDataTemplate, with referencing your dataTemplate dealing with the "Checked" property.
 */
class ExportedByVisuDialog CATVidGridViewAutoCheckColumn : public CATVidGridViewColumn
{
  CATDeclareClass;
public:
  /** Default constructor */
  CATVidGridViewAutoCheckColumn();
  /** Destructor */
  ~CATVidGridViewAutoCheckColumn();
  
	/**
	* Modify the flag that determines whether consumed press event must be
	* taken into account for selection.
	*
	* @param: i_flag: the new value of the flag (should be 0 or 1)
	*/
	virtual void SetHandleConsumedPressEventFlag(int i_flag);

	/**
	* Modify the flag that determines whether row selection is binded to model items "Checked" property.	
	* @param: i_flag: the new value of the flag. Default value is FALSE.
	*/
	void SetBindWithSelectionFlag(CATBoolean i_bindWithSelectionFlag);

	/**
	* Ge the flag that determines whether row selection is binded to model items "Checked" property.	
	*/
	CATBoolean GetBindWithSelectionFlag();


private:

	CATBoolean _bindWithSelectionFlag;
};

#endif
