/**
 * This header only exists in order to avoid compilation errors in client framework (FreeStyleShapeUI.tst).
 * Once these frameworks are migrated to CATPGMCreateInterpolPtToNurbsCrv.h, the current header
 * must be deleted.
 */

#include "CATPGMCreateInterpolPtToNurbsCrv.h"
