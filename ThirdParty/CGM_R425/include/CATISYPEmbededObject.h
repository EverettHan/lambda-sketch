#ifndef _CATISYPEmbededObject_H
#define _CATISYPEmbededObject_H

#include <CATBaseUnknown.h>
#include <CATStyleProcessor.h>

extern ExportedByCATStyleProcessor IID IID_CATISYPEmbededObject;

/**
 * Interface used by StyleProcessor to not treat some part of a
 * XML style sheet, and add directly the XML node to the object, 
 * to let it treat it its way. If an object generated by introspection
 * by the StyleProcessor implements this interface, it will received
 * raw all the children nodes, without any other treatment. It allows 
 * to embed external XML definition.
 */
class ExportedByCATStyleProcessor CATISYPEmbededObject : public CATBaseUnknown
{
	CATDeclareInterface;
public:
	/**
	 * Get items
	 **/
	virtual CATBaseUnknown_var GetAt( int &iIndex ) = 0;
	/**
	 * Get items count
	 **/
	virtual int GetCount( void ) = 0;
	/**
	 * Add an item
	 **/
	virtual void Append( const CATBaseUnknown_var &iObject ) = 0;
};

CATDeclareHandler( CATISYPEmbededObject, CATBaseUnknown );

#endif

