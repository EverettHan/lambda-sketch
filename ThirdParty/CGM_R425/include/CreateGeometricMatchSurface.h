/**
 * This header only exists in order to avoid compilation errors in client framework (FreeStyleResources).
 * Once these frameworks are migrated to CATPGMCreateGeometricMatchSurface.h, the current header
 * must be deleted.
 */

#include "CATPGMCreateGeometricMatchSurface.h"
