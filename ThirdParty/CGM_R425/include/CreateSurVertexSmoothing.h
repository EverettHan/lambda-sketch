/**
 * This header only exists in order to avoid compilation errors in client frameworks (CATClassAUI).
 * Once these frameworks are migrated to CATPGMCreateSurVertexSmoothing.h, the current header
 * must be deleted.
 */

#include "CATPGMCreateSurVertexSmoothing.h"
