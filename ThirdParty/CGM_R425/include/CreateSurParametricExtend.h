/**
 * This header only exists in order to avoid compilation errors in client frameworks (CATClassAUI, FreeStyleResources).
 * Once these frameworks are migrated to CATPGMCreateSurParametricExtend.h, the current header
 * must be deleted.
 */

#include "CATPGMCreateSurParametricExtend.h"
