/**
 * This header only exists in order to avoid compilation errors in client frameworks (FreeStyleResources).
 * Once these frameworks are migrated to CATPGMCreateBlendCurve.h, the current header
 * must be deleted.
 */

#include "CATPGMCreateBlendCurve.h"
