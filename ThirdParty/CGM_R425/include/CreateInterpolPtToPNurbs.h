/**
 * This header only exists in order to avoid compilation errors in client frameworks (FreeStyleResources and FreeStyleShapeUI).
 * Once these frameworks are migrated to CATPGMCreateInterpolPtToPNurbs.h, the current header
 * must be deleted.
 */

#include "CATPGMCreateInterpolPtToPNurbs.h"
