<meta charset="UTF-8"/>
EvalNewExpression="对新的表达式进行求值：\n";
ErrEvalExp="在表达式的求值期间捕获错误：\n ";
ErrEvalOneExp="其中一个包含的知识工程逻辑表达式出错：\n ";
For_While_Loop="迭代（用于）";
For_Each_Loop="迭代（用于每个）";
Iterations="修订版";
If_Then_Else="If/Else";
If_Then="If";
If="If";
Else="Else";
