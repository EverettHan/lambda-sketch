<meta charset="UTF-8"/>
EvalNewExpression="Оценка нового выражения: \n";
ErrEvalExp="Ошибка во время оценки выражения:\n ";
ErrEvalOneExp="Ошибка в одном из содержащихся логических выражений базы знаний:\n ";
For_While_Loop="Итерация (for)";
For_Each_Loop="Итерация (foreach)";
Iterations="Итерации";
If_Then_Else="If/Else";
If_Then="If";
If="If";
Else="Else";
