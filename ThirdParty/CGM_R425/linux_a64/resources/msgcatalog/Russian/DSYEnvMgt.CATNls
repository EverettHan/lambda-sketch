<meta charset="UTF-8"/>
HelpDSYEnvMgt.txt=" 
DSYEnvMgt -create|-del [-e EnvName] [-d EnvDirPath|INSTALL] [-p ConcatPath] [-from Env|EnvFullPath [-append]] -h
  -create or -del       : чтобы создать или удалить файл среды.
  -e EnvName            : имя среды для создания или удаления (необязательно).
                          Имя по умолчанию: Env.
  -d EnvDirPath|INSTALL : папка для хранения файлов среды (необязательно).
                          Если не используется, EnvDir = \"/p1\".
                          Если -d INSTALL, EnvDir = \"/p2\".
                          Если -d EnvDirPath, EnvDir = EnvDirPath.
  -p ConcatPath         : путь объединения. Можно указать несколько путей (только для создания, необязательно)
                          Значение по умолчанию: \"/p3\".
                          Когда используется с -from, добавляет ConcatPath в начало пути установки.
  -from Env|EnvFullPath : Чтобы создать новый файл среды на основе существующего (только для создания, необязательно).
                          Вы можете указать имя среды или полный путь к ней.
                          Например: 
                             DSYEnvMgt -create -e Env2 -from \"/p2\Env.txt\" создаст Env2.txt в \"/p1\".
                          или 
                             DSYEnvMgt -create -e Env2 -from Env -d INSTALL создаст Env2.txt в \"/p2\".
  -append               : если используется с параметрами -from и -p, ConcatPath будет добавлен в конец объединения.
                          Если не используется, но используются параметры -from и -p, ConcatPath будет добавлен в начало объединения.
  -h                    : эта справка.";

MissingValue_e="После параметра -e должно следовать имя файла среды.";
MissingValue_d="После параметра -d необходимо указать имя пути к папке, в которой будет создан файл среды.";
MissingValue_p="После параметра -p должен следовать путь объединения.";
MissingValue_from="После параметра -from должно следовать имя или путь к файлу среды, который требуется скопировать.";
InvalidArg="Параметр /p1 использовать не разрешается.";
BadOptions_createdel="Параметры -create и -del нельзя использовать вместе.";
MissingOptions_createordel="Один из параметров -create или -del является обязательным.";
MissingConcatPath="Невозможно извлечь путь объединения по умолчанию. Используйте параметр -p, чтобы указать путь объединения.";
DirNotExist="Каталог \"/p1\" не существует.";
NotAdmin="Для создания файла среды в \"/p1\" необходимы повышенные полномочия.";
BadFrom_NotExist="Параметр -from не допустим: путь /p1 не существует.\n";
CreateEnvOK="Файл среды \"/p1.txt\" создан.\n";
CreateEnvKO="DSYEnvMgt не удалось создать \"/p1.txt\".\n";
CreateEnv="Создание файла среды \"/p1.txt\".\n";
CreateDirFailed="DSYEnvMgt не удалось создать \"/p1.txt\".";
CreateDirFailedNotAdmin="DSYEnvMgt не удалось создать \"/p1.txt\". Проверьте свои права администратора.";
DeleteEnv="Удаление файла среды \"/p1.txt\".";
DeleteEnvOK="Файл среды \"/p1.txt\" удален.";
DeleteEnvKO="DSYEnvMgt не удалось удалить \"/p1.txt\".";
CreateFromEnv="Создание файла среды \"/p1.txt\" на основе \"/p2\".";
BadOptiond="Параметр -d не допустим:\nпосле -d должны следовать слово INSTALL или допустимый путь.";
BadOptione="Параметр -e не допустим:\nпосле -e должно следовать имя среды.";
BadOptionp="Параметр -p не допустим:\nпосле -p должен следовать путь.";
BadOptionfrom="Параметр -from не допустим:\nпосле -from должны следовать допустимое имя среды или полный путь к имени среды.";
MissingArgs="Параметры отсутствуют.\n";
NotAdmin2="Для использования параметра /p1 необходимы повышенные полномочия.\n";
