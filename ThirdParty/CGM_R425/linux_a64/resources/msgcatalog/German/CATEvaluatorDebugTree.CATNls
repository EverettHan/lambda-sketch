<meta charset="UTF-8"/>
EvalNewExpression="Neuer Ausdruck wird ausgewertet: \n";
ErrEvalExp="Bei der Auswertung des Ausdrucks ist ein Fehler aufgetreten.\n ";
ErrEvalOneExp="Einer der enthaltenen logischen Knowledge-Ausdrücke enthält einen Fehler:\n ";
For_While_Loop="Iteration (for)";
For_Each_Loop="Iteration (foreach)";
Iterations="Iterationen";
If_Then_Else="If/Else";
If_Then="If";
If="If";
Else="Else";
